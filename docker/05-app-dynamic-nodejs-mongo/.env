# --- MongoDB Root Configuration (for initialization) ---
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example

# --- Application-specific Database Configuration ---
# The Node.js app will use these credentials to connect.
MONGO_DATABASE=appdb
MONGO_USER=appuser
MONGO_PASSWORD=apppass
